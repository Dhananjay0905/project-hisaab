org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
kotlin.incremental=false
# receive_sharing_intent (and some other Flutter plugins) hardcode sourceCompatibility=1.8
# in their own build.gradle. AGP finalizes that value before any root-project override
# can run, so we cannot force Java 17 from outside the plugin's build file.
# This downgrades the Kotlin plugin's strict JVM-target consistency check from an
# error to a warning so the build succeeds without touching plugin internals.
kotlin.jvm.target.validation.mode=WARNING
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
